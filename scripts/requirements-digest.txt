# Daily Digest generator dependencies
# Pinned to major version — allows all bug-fix/security updates,
# blocks breaking major-version changes.
# Update upper bound only when the new major version is confirmed
# backwards-compatible with this script.
anthropic>=0.20,<2
openai>=1.0,<2
google-genai>=0.8,<2
tavily-python>=0.5,<2
exa-py>=1.0,<2
beautifulsoup4>=4.0,<5
feedparser>=6.0,<7
